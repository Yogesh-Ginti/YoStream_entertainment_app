// Exporting the base URL for the TMDB API stored in the environment variable `VITE_TMDB_BASE_URL`
export const tmdb_base_url = import.meta.env.VITE_TMDB_BASE_URL;

// Exporting the base URL for the backend API stored in the environment variable `VITE_BACKEND_BASE_URL`
export const backend_base_url = import.meta.env.VITE_BACKEND_BASE_URL;

// Exporting the API key for the TMDB API stored in the environment variable `VITE_TMDB_API_KEY`
export const apiKey = import.meta.env.VITE_TMDB_API_KEY;
